# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database
sqlalchemy==2.0.35
psycopg2-binary==2.9.10
cryptography==43.0.1
alembic==1.13.3

# Validation & Settings
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# HTTP client (for tests / drone simulator)
httpx==0.27.2

# Image processing — Python 3.13 compatible
# numpy 2.x is required for Python 3.13 wheels; ultralytics>=8.3.2 supports it
numpy>=2.0.0
opencv-python-headless>=4.10.0.84
pillow>=11.0.0

# ML — YOLOv8 (>=8.3.2 supports numpy 2.x)
ultralytics>=8.3.2

# Phase 5 — Celery + Redis
celery[redis]==5.4.0
redis==5.2.0

# Phase 5 — JWT Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Phase 5 — Rate limiting
slowapi==0.1.9

# Phase 6 — PDF reports
reportlab==4.2.5
